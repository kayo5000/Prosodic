# Copy to .env.local and fill in per environment.
# EXPO_PUBLIC_* vars are inlined at build time — never put secrets here.

# Sync backend base URL. Left unset in v1 dev until a host is chosen (build order step 1).
EXPO_PUBLIC_SYNC_API_URL=
